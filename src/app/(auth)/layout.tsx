/**
 * Auth Layout
 * Minimal layout for login, forgot-password, and reset-password pages.
 * No sidebar. No header. Full-screen.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
